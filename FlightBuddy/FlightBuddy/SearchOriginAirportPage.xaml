<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FlightBuddy.SearchOriginAirportPage" Title="Search flight origin">

    <StackLayout Orientation="Vertical">
        <SearchBar x:Name="originAirportSearch"
                   TextChanged="SearchAirports_TextChanged" Placeholder="Type your origin airport"/>

        <ListView x:Name="airportSuggestions" ItemTapped="SetOriginAirport">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding Name}" 
                              Detail="{Binding Code}"
                              TextColor="{StaticResource blueColor}" 
                              DetailColor="Black"></TextCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <ActivityIndicator x:Name="ActivityIndicator" IsRunning="{Binding IsBusy}" 
                           VerticalOptions="Center" HorizontalOptions="Center" Color="{StaticResource blueColor}"></ActivityIndicator>
    </StackLayout>

</ContentPage>