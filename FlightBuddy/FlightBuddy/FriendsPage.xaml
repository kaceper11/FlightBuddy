<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FlightBuddy.FriendsPage"
             NavigationPage.HasNavigationBar="false">
<Grid>
        <ListView x:Name="friendsListView" IsPullToRefreshEnabled="True"  Refreshing="friendsListView_Refreshing" ItemTapped="UserFriend_Clicked" RowHeight="80">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <StackLayout VerticalOptions="FillAndExpand" Orientation="Horizontal" Padding="10">

                        <Image Aspect="AspectFill"
                               HeightRequest ="75" WidthRequest="75"
                               Source="{Binding ProfilePictureUrl}" />

                        <Label Text="{Binding Name}" YAlign="Center" Font="20" TextColor="{StaticResource blueColor}"/>
                    </StackLayout>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
    <ActivityIndicator x:Name="ActivityIndicator" IsRunning="{Binding IsBusy}" 
                   VerticalOptions="Center" HorizontalOptions="Center" Color="{StaticResource blueColor}"></ActivityIndicator>
</Grid>

</ContentPage>